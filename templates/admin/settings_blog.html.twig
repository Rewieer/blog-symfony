{% extends "layout/admin.html.twig" %}
{% block body %}

  <div id="page-wrapper">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="page-header">Paramètres du blog</h1>

        {% form_theme form 'form/admin.fields.html.twig' %}
        {{ form_start(form) }}
        {{ form_rest(form) }}

        <input type="submit" class="btn btn-primary" value="Sauvegarder" />
        {{ form_end(form) }}
      </div>
      <!-- /.col-lg-12 -->
    </div>
  </div>
{% endblock %}